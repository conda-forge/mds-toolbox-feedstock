{% set name = "mds-toolbox" %}
{% set version = "2.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/mds_toolbox-{{ version }}.tar.gz
  sha256: 97bc76f9ac28dfa97750f38385356fde1535d55aad192a15c9152fdb2b4b6f46

build:
  noarch: python
  entry_points:
    - mds = mds.mng:start_from_command_line_interface
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - poetry-core
    - pip
  run:
    - python >={{ python_min }},<3.13
    - boto3 >=1.37.4,<2.0.0
    - copernicusmarine >=2.2.2
    - click >=8.1.8,<9.0.0
    - zarr >=3.0
    - __unix

test:
  imports:
    - mds
  commands:
    - pip check
    - mds --help
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://pypi.org/project/mds-toolbox
  summary: A custom CMCC library to list and download data from the Marine Data Store
  license: MPL-2.0
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - anto6715
    - aded
